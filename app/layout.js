import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import ClientLayout from "@/components/ClientLayout";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "White Ink Capital - Live Stock Market Dashboard",
  description: "White Ink Capital is a modern stock market dashboard designed to provide real-time financial insights, live stock prices, cryptocurrency trends, commodities data, and market news in one unified platform. The website enables users to track market movements, analyze performance, and stay informed with accurate and interactive financial data visualization.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ClientLayout>
          {children}
        </ClientLayout>
      </body>
    </html>
  );
}
